# Backend runtime config (Render)
# Comma-separated list of allowed frontend origins
FRONTEND_URLS=https://your-vercel-app.vercel.app,https://www.your-domain.com

# Optional single-origin variable (kept for compatibility)
FRONTEND_URL=https://your-vercel-app.vercel.app

# Optional regex for preview deployments on Vercel
# Example: https://.*-your-team\.vercel\.app
FRONTEND_ORIGIN_REGEX=

# Firebase Admin service account path inside Render filesystem
# (if using a file mounted at deploy/runtime)
FIREBASE_CREDENTIALS_PATH=/etc/secrets/firebase-service-account.json

# Set to 1 only for local development without Firebase
DISABLE_FIREBASE=0
